odoo.define('sd_facturacion_en_linea_correo_v13.inherit_models', function (require) {
    'use strict';

    var models = require('point_of_sale.models');
    var exports = {};

    var PosModel = models.PosModel.prototype;

    models.load_fields("pos.config", ['sd_fcb_es_electronico', 'sd_download_pdf']);

    models.PosModel = models.PosModel.extend({
//        initialize: function (session, attributes) {
//            PosModel.initialize.call(this, session, attributes);
//
//        },

        push_and_invoice_order: function (order) {
        var self = this;
        var invoiced = new Promise(function (resolveInvoiced, rejectInvoiced) {
            if(!order.get_client()){
                rejectInvoiced({code:400, message:'Missing Customer', data:{}});
            }
            else {
                var order_id = self.db.add_order(order.export_as_JSON());
                self.flush_mutex.exec(function () {
                    var done =  new Promise(function (resolveDone, rejectDone) {
                        // send the order to the server
                        // we have a 30 seconds timeout on this push.
                        // FIXME: if the server takes more than 30 seconds to accept the order,
                        // the client will believe it wasn't successfully sent, and very bad
                        // things will happen as a duplicate will be sent next time
                        // so we must make sure the server detects and ignores duplicated orders

                        var transfer = self._flush_orders([self.db.get_order(order_id)], {timeout:30000, to_invoice:true});

                        transfer.catch(function (error) {
                            rejectInvoiced(error);
                            rejectDone();
                        });

                        // on success, get the order id generated by the server
                        transfer.then(function(order_server_id){
                            // generate the pdf and download it
                            if (order_server_id.length && !order.is_to_email()) {

                                console.log('config actual',self.config)
                                if (self.config.sd_fcb_es_electronico && self.config.sd_download_pdf){
                                    self.chrome.do_action('sd_facturacion_en_linea_correo_v13.sd_action_imprimir_facturacion_siat_rollo_pos',{additional_context:{
                                        active_ids:order_server_id,
                                    }}).then(function () {
                                        resolveInvoiced(order_server_id);
                                        resolveDone();
                                    }).guardedCatch(function (error) {
                                        rejectInvoiced({code:401, message:'Backend Invoice', data:{order: order}});
                                        rejectDone();
                                    });
                                }else if (self.config.sd_download_pdf){
                                    self.chrome.do_action('point_of_sale.pos_invoice_report',{additional_context:{
                                        active_ids:order_server_id,
                                    }}).then(function () {
                                        resolveInvoiced(order_server_id);
                                        resolveDone();
                                    }).guardedCatch(function (error) {
                                        rejectInvoiced({code:401, message:'Backend Invoice', data:{order: order}});
                                        rejectDone();
                                    });
                                }else if (!self.config.sd_download_pdf){
                                    resolveInvoiced(order_server_id);
                                    resolveDone();
                                }
                            } else if (order_server_id.length) {
                                resolveInvoiced(order_server_id);
                                resolveDone();
                            } else {
                                // The order has been pushed separately in batch when
                                // the connection came back.
                                // The user has to go to the backend to print the invoice
                                rejectInvoiced({code:401, message:'Backend Invoice', data:{order: order}});
                                rejectDone();
                            }
                        });
                        return done;
                    });
                });
            }
        });

        return invoiced;
    },
    });

    return exports;



//    var CustomModels = models.PosModel.extend({
//        push_and_invoice_order: function(order) {
//            // Call the base method from the parent class
////            this._super(order);
//            console.log('Extended push_and_invoice_order method');
//
//            // Add your custom code here to extend the method
//            if (self.config.sd_bool_test){
//                self.chrome.do_action('sd_facturacion_en_linea_correo_v13.sd_action_imprimir_facturacion_siat_rollo_pos',{additional_context:{
//                    active_ids:order_server_id,
//                }}).then(function () {
//                    resolveInvoiced(order_server_id);
//                    resolveDone();
//                }).guardedCatch(function (error) {
//                    rejectInvoiced({code:401, message:'Backend Invoice', data:{order: order}});
//                    rejectDone();
//                });
//            }else{
//                self.chrome.do_action('point_of_sale.pos_invoice_report',{additional_context:{
//                    active_ids:order_server_id,
//                }}).then(function () {
//                    resolveInvoiced(order_server_id);
//                    resolveDone();
//                }).guardedCatch(function (error) {
//                    rejectInvoiced({code:401, message:'Backend Invoice', data:{order: order}});
//                    rejectDone();
//                });
//            }
//        }
//    });
//    return {CustomModels: CustomModels, };

    // Replace the original PosModel with the custom derived class
//    models.PosModel = CustomModels;
});

